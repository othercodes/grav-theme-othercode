name: Intro Styles
description: Intro section styles for the otherCode theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#ffffff"
    background-image:
      type: input.imagepicker
      label: Background Image
      default: 'gantry-media://header/img01.jpg'
    background-overlay:
      type: select.select
      label: Background Overlay
      description: 'Enables the linear gradient overlay made of accent colors.'
      placeholder: 'Select...'
      default: enabled
      options:
        enabled: Enabled
        disabled: Disabled
    background-parallax:
      type: select.select
      label: Parallax Effect
      description: 'Enables the CSS based parallax effect.'
      placeholder: 'Select...'
      default: enabled
      options:
        enabled: Enabled
        disabled: Disabled
    text-color:
      type: input.colorpicker
      label: Text
      default: "#444444"
    padding:
      type: select.select
      label: Padding
      description: 'Enables the padding over this section.'
      placeholder: 'Select...'
      default: disabled
      options:
        enabled: Enabled
        disabled: Disabled